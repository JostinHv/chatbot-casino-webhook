const { ISessionRepository } = require('./ISessionRepository');
const { IEntityRepository } = require('./IEntityRepository');
const { IResponseRepository } = require('./IResponseRepository');
const { IHistoryRepository } = require('./IHistoryRepository');

module.exports = {
  ISessionRepository,
  IEntityRepository,
  IResponseRepository,
  IHistoryRepository
}; 